/*

    _e.waitUntil(caches.open("armadillo").then(function (cache) {
        return cache.addAll(FILES_TO_CACHE);
    }));

*/

const getCachedTitles = () => {
	caches.open("armadillo_content").then(cache => {
		
	})
}